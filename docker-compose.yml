services:
  estimotebeaconreceiver:
    image: ${DOCKER_REGISTRY-}estimotebeaconreceiver
    privileged: true
    user: root
    volumes:
      - /var/run/dbus:/var/run/dbus
      - /dev:/dev 
      - /sys:/sys
    environment:
      - BeaconReceiverSettings__AdapterName=/org/bluez/hci0
      - 'UDEV=1'
    network_mode: "host"
    build:
      context: EstimoteBeaconReceiver
      dockerfile: Dockerfile
